---
deployment:
  tasks:
    - export DEPLOYPATH="$HOME/public_html/"
    - /bin/cp -R * "$DEPLOYPATH"
    - /bin/cp -R .htaccess "$DEPLOYPATH"
    - /bin/rm -f "$DEPLOYPATH""aircond-expert-site (1).zip"
